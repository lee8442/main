<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin.order.selectOrderList">
	<!--주문 목록 조회 -->
	<select id="OrderList" resultType="OrderVO">
		<![CDATA[select a.code, a.member_id, a.product_code, a.amount, a.product_order_date, b.state as delivery_code, c.state as cancle_code, a.delivery_address 
		from product_order a, product_order_delivery b, product_order_cancel c
		where a.delivery_code = b.code and a.cancle_code = c.code]]>
	</select>
</mapper>