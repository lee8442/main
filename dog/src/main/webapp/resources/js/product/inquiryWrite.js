/*function  QnAformCheck(){
	var question = document.QnA_board.question.value;
	var email = document.QnA_board.email.value;
	var title = document.QnA_board.title.value;
	var QnA_content = document.QnA_board.QnA_content.value;
	if(question == null || question==""){
		window.alert("내용을 입력하세요.");
		document.QnA_board.question.focus();
		return false;
	}
	if(email==null || email==""){
		window.alert("이메일을 입력하세요.");
		document.QnA_board.email.focus();
		return false;
	}
	if(title==null || title==""){
		window.alert("제목을 입력하세요.");
		document.QnA_board.title.focus();
		return false;
	}
	if(QnA_content == null || QnAcontet == ""){
		window.alert("내용을 입력하세요.");
		document.QnA_board.QnA_content.focus();
		return false;
	}
}*/

